import { Routes } from '@angular/router';
import { StationComponent } from './components/station/station.component';

export const climateRoutes: Routes = [
  {
    path: '',
    component: StationComponent
  }
];
